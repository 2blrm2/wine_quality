from setuptools import setup, find_packages

setup(
    name="src",
    version="0.0.1",
    description= "its our own package for wine quality project",
    author = "Balram Chaudhary",
    packages = find_packages(),
    license = "MIT"
)